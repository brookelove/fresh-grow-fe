import { Link } from 'react-router-dom';
// import { capitalizeFirstLetter } from '../../utils/helpers';

export default function Header(){
    return (
        <header>
            <nav>
                <h1>Hamburger in smaller than an ipad</h1>
            </nav>
        </header>
    )
}